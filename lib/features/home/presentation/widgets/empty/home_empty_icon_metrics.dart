final class HomeEmptyIconMetrics {
  const HomeEmptyIconMetrics({
    required this.outerSize,
    required this.baseSize,
    required this.innerSize,
  });

  final double outerSize;
  final double baseSize;
  final double innerSize;
}
