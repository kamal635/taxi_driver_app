/// Placeholder file kept to reserve the listeners directory for auth flows.
///
/// The login page currently handles its own authenticated navigation because it
/// owns the text controllers and submit lifecycle.
final class LoginStateListener {
  const LoginStateListener._();
}
