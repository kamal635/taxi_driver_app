include: package:very_good_analysis/analysis_options.yaml

analyzer:
  plugins:
    - custom_lint
linter:
  rules:
    # In apps, this rule can be too noisy. We keep the rest strict.
    public_member_api_docs: false
